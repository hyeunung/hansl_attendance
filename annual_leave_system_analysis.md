# 연차 시스템 분석 보고서

## 현재 연차 관리 시스템

### 1. 관련 엣지 펑션들

#### `annual_year_update` (1월 1일 전체 초기화용)
- **목적**: 새해 1월 1일에 전체 직원 연차 일괄 업데이트
- **실행 방식**: **수동 실행** (관리자가 직접 호출해야 함)
- **자동 실행**: ❌ **없음** (cron job 설정 안됨)
- **로직**:
  ```typescript
  // 잘못된 로직 (현재)
  - 0년차: 15개 기본값 ❌
  - 1~2년차: 15개 고정
  - 3년차 이상: 15 + Math.floor((근속년수-1)/2)
  
  // 올바른 로직 (법정 기준)
  - 1년 미만: 월차 (매월 1개씩, 최대 11개)
  - 1년 이상: 15개
  - 3년차부터: 2년마다 1개 추가 (최대 25개)
  ```
- **문제점**: 
  1. 자동 실행 설정 없음
  2. 신입 월차 계산 로직 없음
  3. 사용연차/잔여연차를 0으로 초기화 (이월 처리 없음)

#### `anniversary_check` (입사 12개월 완성시)
- **목적**: 입사 정확히 1년 되는 날 연차 15개 부여
- **실행 방식**: **수동 실행** (관리자가 직접 호출)
- **자동 실행**: ❌ **없음**
- **로직**: 입사 12개월 완성시 무조건 15개 부여
- **문제점**: 자동 실행 설정 없음

#### `update_used_annual_leave` (사용연차 계산)
- **목적**: 사용연차 재계산 및 잔여연차 업데이트
- **실행 시점**: leave 테이블 변경시 또는 수동 호출

### 2. 자동 실행 여부

**❌ 현재 자동 실행 설정이 전혀 없습니다**

- Supabase의 pg_cron 설정 없음
- 프론트엔드에서 주기적 호출 없음
- 트리거나 스케줄러 없음

### 3. 문제점 정리

#### 1월 1일 연차 초기화
- **자동 실행 안됨** → 관리자가 수동으로 실행해야 함
- **신입 월차 처리 안됨** → 1년 미만 직원도 15개 부여
- **이월 처리 없음** → 전년도 잔여연차 소멸

#### 입사일 기반 연차
- **anniversary_check 자동 실행 안됨** → 입사 1년차 직원 연차 누락 가능
- **월차 자동 발생 안됨** → 신입 월차 수동 처리 필요

### 4. 개선 방안

#### 즉시 필요한 조치
1. **1월 1일 자동 실행 설정**
   - Supabase pg_cron 또는
   - GitHub Actions 스케줄러 또는
   - 외부 cron 서비스 활용

2. **월차 자동 발생 로직 추가**
   - 매월 1일 신입 월차 계산 및 부여
   - 1개월 만근 체크 로직

3. **입사 기념일 자동 체크**
   - 매일 실행하여 입사 1년 완성자 체크
   - 자동으로 15개 연차 부여

#### 권장 구현 방식
```sql
-- Supabase pg_cron 예시
SELECT cron.schedule(
  'annual-leave-reset',
  '0 0 1 1 *', -- 매년 1월 1일 00:00
  $$
    -- annual_year_update 엣지 펑션 호출 로직
  $$
);

SELECT cron.schedule(
  'monthly-leave-grant',
  '0 0 1 * *', -- 매월 1일 00:00
  $$
    -- 신입 월차 부여 로직
  $$
);

SELECT cron.schedule(
  'anniversary-check',
  '0 0 * * *', -- 매일 00:00
  $$
    -- anniversary_check 엣지 펑션 호출 로직
  $$
);
```

## 결론

현재 시스템은 **모든 연차 관련 작업이 수동**으로 이루어지고 있습니다. 
1월 1일 연차 초기화, 입사 기념일 체크, 월차 부여 등 모두 관리자가 직접 실행해야 합니다.

자동화 설정이 시급히 필요합니다.