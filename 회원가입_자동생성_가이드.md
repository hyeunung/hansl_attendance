# 회원가입 시 employees 테이블 자동 생성 가이드

## 개요

회원가입 시 `employees` 테이블에 자동으로 레코드가 생성되도록 Database Trigger를 추가했습니다.
이를 통해 `auth.users.id`와 `employees.id`가 동기화되어 박정현 케이스와 같은 문제를 해결합니다.

## 변경 사항

### 1. Database Migration
**파일**: `supabase/migrations/20251215_auto_create_employee_on_signup.sql`

- `employees` 테이블에 `auth_user_id` 컬럼 추가
- `auth.users`에 사용자 생성 시 자동으로 `employees` 레코드 생성하는 트리거 추가
- 기존 레코드의 `auth_user_id` 자동 업데이트

### 2. 회원가입 로직 수정
**파일**: `lib/screens/auth/login_screen.dart`

- 기존: `employees` 테이블에 등록되어 있어야만 회원가입 가능
- 변경: `employees` 테이블 체크 제거, 회원가입 시 자동 생성

## 동작 방식

### 회원가입 시

1. 사용자가 앱/웹에서 회원가입
2. `auth.users` 테이블에 레코드 생성
3. **트리거 자동 실행**: `handle_new_user()` 함수 호출
4. `employees` 테이블에 자동으로 레코드 생성
   - `id` = `auth.users.id` (동기화)
   - `auth_user_id` = `auth.users.id`
   - `name` = 회원가입 시 입력한 이름 (또는 이메일에서 추출)
   - `email` = 회원가입 시 입력한 이메일
   - 기본 연차: 15일

### 기존 레코드가 있는 경우

- 이메일로 매칭된 기존 레코드가 있으면:
  - `id`를 `auth.users.id`로 업데이트 (동기화)
  - `auth_user_id` 설정

## 적용 방법

### 1. Migration 실행

```bash
# Supabase CLI 사용
supabase db push

# 또는 직접 SQL 실행
psql -h [host] -U [user] -d [database] -f supabase/migrations/20251215_auto_create_employee_on_signup.sql
```

### 2. 앱 업데이트

Flutter 앱의 회원가입 로직이 이미 수정되었으므로, 앱을 재배포하면 됩니다.

### 3. 웹 프로젝트 (hanslwebapp)

웹 프로젝트의 회원가입 로직도 동일하게 수정이 필요합니다:
- `employees` 테이블 체크 제거
- 회원가입 시 `auth.users`만 생성하면 트리거가 자동으로 `employees` 레코드 생성

## 장점

1. **ID 동기화**: `auth.users.id`와 `employees.id`가 항상 일치
2. **자동화**: 관리자가 수동으로 `employees` 테이블에 추가할 필요 없음
3. **일관성**: 모든 사용자가 `employees` 테이블에 자동 등록됨
4. **박정현 케이스 해결**: ID 불일치 문제 해결

## 주의사항

1. **기존 데이터**: 기존 `employees` 레코드의 `id`가 `auth.users.id`와 다를 수 있음
   - Migration에서 이메일로 매칭하여 `auth_user_id` 업데이트
   - 필요시 `id`도 업데이트 (주의 필요)

2. **관리자 설정**: 부서, 직급 등은 회원가입 후 관리자가 수동으로 설정해야 함

3. **연차 설정**: 기본값 15일로 설정되며, 필요시 관리자가 조정

## 테스트

1. 새 사용자로 회원가입
2. `employees` 테이블 확인:
   ```sql
   SELECT id, name, email, auth_user_id 
   FROM employees 
   WHERE email = 'test@hansl.com';
   ```
3. `auth.users`와 `employees`의 `id`가 일치하는지 확인










